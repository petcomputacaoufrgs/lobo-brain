#include <stdio.h>
#include <iostream>
#include <vector>
#include <string>
#include "move.hpp"

using namespace std;


int main()
{
    int player_pos = 0;

    vector<vector<int>> possible_mov = tapatanMoves(player_pos);

    //for(vector<vector<int>>::iterator)

    return 0;
}